/* eslint-disable no-undef */
describe('app', () => {
  describe('GET /order/submit', () => {
    it('example test', () => {
      expect(1).toBe(1);
    });
    /**
     * TODO
     * - Test all order matching scenarios
     */
  });
});
